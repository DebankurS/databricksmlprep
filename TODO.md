# TODO — Databricks ML Certification Project-Specific Fixes

## Setup — Local Stack (no cloud account needed)
- [ ] Install PySpark (`pip install pyspark`) — Spark/DataFrames practice
- [ ] Install delta-spark (`pip install delta-spark`) — Delta Lake practice
- [ ] Run MLflow local server (`pip install mlflow && mlflow server`) — tracking/registry/deployment
- [ ] Run Unity Catalog OSS via Docker (`github.com/unitycatalog/unitycatalog`) — UC practice
- [ ] Use floci for job scheduler simulation (Lambda + EventBridge or Pub/Sub)
